---
import ArchivePanel from "@components/ArchivePanel.svelte";
import I18nKey from "@i18n/i18nKey";
import { i18n } from "@i18n/translation";
import MainGridLayout from "@layouts/MainGridLayout.astro";
import { getSortedPostsList } from "@utils/content-utils";

const sortedPostsList = await getSortedPostsList();

// 1. 修复类型兼容性：将 category 的 null 转换为 undefined
const posts = sortedPostsList.map((post) => ({
    ...post,
    data: {
        ...post.data,
        category: post.data.category === null ? undefined : post.data.category,
    },
}));

// 2. 提取所有标签 (去重)
const tags = [...new Set(posts.flatMap(post => post.data.tags))];

// 3. 提取所有分类 (去重且过滤掉 undefined)
const categories = [...new Set(posts.map(post => post.data.category).filter((c): c is string => typeof c === "string"))];

// 4. 提取所有项目 (去重且过滤掉空值)
const projects = [...new Set(posts.map(post => post.data.project).filter((p): p is string => typeof p === "string" && p !== ""))];
---

<MainGridLayout title={i18n(I18nKey.archive)}>
    <!-- 引入右侧边栏布局管理器 -->
    <script>
        import('../scripts/right-sidebar-layout.js');
    </script>

    <ArchivePanel
            sortedPosts={posts}
            tags={tags}
            categories={categories}
            projects={projects}
            client:only="svelte"
    ></ArchivePanel>
</MainGridLayout>
